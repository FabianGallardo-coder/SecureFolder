<Application x:Class="SecureFolder.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SecureFolder.App"
             xmlns:converters="clr-namespace:SecureFolder.App.Converters"
             xmlns:tb="http://www.hardcodet.net/taskbar"
             ShutdownMode="OnExplicitShutdown"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!-- Converters -->
        <converters:BoolToVisibleConverter x:Key="BoolToVisibleConverter" />
        <converters:CountToVisibleConverter x:Key="CountToVisibleConverter" />

        <!-- Global Styles -->
        <Style TargetType="Window">
            <Setter Property="Background" Value="#1a1a2e"/>
        </Style>

        <!-- Tray Icon -->
        <tb:TaskbarIcon x:Key="SecureFolderTray"
                        ToolTipText="SecureFolder"
                        Visibility="Visible">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Abrir" Click="TrayMenu_Open_Click"/>
                    <Separator/>
                    <MenuItem Header="Bloquear todas" Click="TrayMenu_LockAll_Click"/>
                    <Separator/>
                    <MenuItem Header="Salir" Click="TrayMenu_Exit_Click"/>
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Application.Resources>
</Application>
